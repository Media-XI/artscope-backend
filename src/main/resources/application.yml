spring:
  profiles:
    active: default # default profile
    include:
      - mysql
      - redis
      - jwt
      - flyway
      - oauth
      - s3

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    com.example : DEBUG